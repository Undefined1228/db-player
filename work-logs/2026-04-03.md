# 2026-04-03 작업 로그

## MySQL 스키마 트리 컬럼/인덱스/외래키 미표시 버그 수정

- 변경 파일: `src/main/db/table.ts`, `src/renderer/src/components/schema/SchemaTree.svelte`
- 내용:
  - mysql2가 `information_schema` 쿼리 결과를 대문자 키(`TABLE_NAME`, `COLUMN_NAME` 등)로 반환하는데, 코드에서 소문자(`r.table_name`)로 접근하여 모든 값이 `undefined`가 되는 버그 수정 → 쿼리에 소문자 AS 별칭 명시
  - `SchemaTree.svelte`에서 Indexes/Views 섹션 표시 조건이 `dbType === 'postgresql'`으로 되어 있어 MySQL에서 미표시 → `dbType !== 'sqlite'`로 변경 (인덱스 생성 메뉴 포함)
- 커밋: ✅ fix : MySQL information_schema 쿼리 소문자 별칭 명시 / fix : MySQL getLocks 권한 오류 처리 및 SchemaTree MySQL 지원

## 고정 컬럼 사이 틈으로 스크롤 내용 보이는 문제 수정

- 변경 파일: `src/renderer/src/components/data-viewer/DataViewerTable.svelte`
- 내용: 체크박스/인덱스/고정컬럼 사이 간격으로 스크롤 내용이 보이는 문제 수정. `w-8`/`w-10`(`rem` 단위) 셀 너비가 `getFrozenStyle`의 하드코딩 픽셀값(32/40px)과 달라 sticky 위치가 틀어지는 것이 원인 → 체크박스·인덱스 `<th>`/`<td>` 전체에 `width/min-width/max-width: 32px/40px` 픽셀 고정. 추가로 frozen 바디 셀에 `background-color: var(--color-background)` 인라인 스타일 명시 (CSS 클래스만으로 z-index 렌더링 컨텍스트에서 배경이 보장되지 않는 문제 방지). `getFrozenStyle`에 `bgColor` 파라미터 추가.
- 커밋: ✅ fix : 데이터 뷰어 sticky 컬럼 너비/배경 픽셀 고정

## 연결 등록 다이얼로그 스크롤 및 URL 미리보기 overflow 처리

- 변경 파일: `src/renderer/src/components/connection/ConnectionDialog.svelte`
- 내용: 콘텐츠가 길어질 때 화면을 벗어나는 문제 수정 — `Dialog.Content`에 `max-h-[90vh] flex flex-col` 적용, 폼 영역에 `overflow-y-auto` 추가. URL 미리보기 박스에 `overflow-x-auto whitespace-nowrap` 추가하여 긴 URL이 넘치지 않도록 처리
- 커밋: ✅ fix : ConnectionDialog 스크롤 및 URL overflow 처리
