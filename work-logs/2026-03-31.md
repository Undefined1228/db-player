## DDL 보기 팝업 구현
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/lib/stores/ddl-dialog.ts`, `src/renderer/src/components/DDLDialog.svelte`, `src/renderer/src/lib/actions/sidebar-actions.ts`, `src/renderer/src/App.svelte`
- 내용: `getObjectDDL` 백엔드 구현(table: information_schema → buildPostgresDDL 재사용, view: pg_get_viewdef, matview: pg_matviews, function: pg_get_functiondef). `ddlDialogStore` 신규. `DDLDialog` — readonly textarea + 클립보드 복사 + 닫기. `viewDDL` 액션에서 API 호출 후 스토어 업데이트.
- 커밋: ⏳ 미커밋

## SQL 편집기 탭 열기 구현
- 변경 파일: `src/renderer/src/lib/stores/tabs.ts`, `src/renderer/src/lib/actions/sidebar-actions.ts`, `src/renderer/src/components/QueryWorkspace.svelte`, `src/renderer/src/components/ConnectionContextMenu.svelte`
- 내용: `tabsStore` 신규 (열기/닫기/활성화/쿼리 업데이트). `openSqlEditor`에서 연결·스키마·오브젝트 레벨별 탭 제목 결정 후 tabsStore.openTab 호출. QueryWorkspace에 탭 바 + textarea 에디터 구현, 탭 없으면 empty state 표시. ConnectionContextMenu 연결 레벨에 "SQL 편집기 열기" 추가.
- 커밋: ⏳ 미커밋

## QueryWorkspace 빈 상태 초기 세팅
- 변경 파일: `src/renderer/src/components/QueryWorkspace.svelte`, `src/renderer/src/App.svelte`
- 내용: 기존 QueryEditor + ResultViewer를 단일 QueryWorkspace 컴포넌트로 대체. 현재는 빈 상태(empty state) 표시 — 터미널 아이콘 + 안내 문구 + ⌘T 단축키 힌트.
- 커밋: ⏳ 미커밋

## 테이블 수정(ALTER TABLE) 프론트엔드 구현
- 변경 파일: `src/renderer/src/components/AlterTableDialog.svelte`, `src/renderer/src/components/SchemaObjectContextMenu.svelte`, `src/renderer/src/components/SchemaTree.svelte`, `src/renderer/src/components/Sidebar.svelte`
- 내용: CreateTableDialog를 기반으로 AlterTableDialog 신규 생성. 기존 테이블 컬럼/FK를 초기값으로 로드하고, 삭제/추가/수정 diff를 추적해 alterTable IPC 호출. SchemaObjectContextMenu에 "테이블 수정" 항목 추가. SchemaTree → Sidebar → AlterTableDialog 연결.
- 커밋: ⏳ 미커밋

## 테이블 수정(ALTER TABLE) 백엔드 구현 (PostgreSQL)
- 변경 파일: `src/main/db/ddl-builder.ts`, `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`
- 내용: PostgreSQL ALTER TABLE 백엔드 구현. `ddl-builder.ts`에 `AlterColumnDef`, `AlterFKDef`, `AlterTableParams` 인터페이스 추가 및 `buildAlterTableDDL` 함수 구현(컬럼 삭제/추가/이름변경/타입변경/NULL/DEFAULT, PK 재구성, FK 추가/삭제/수정, 테이블 이름 변경 순서로 SQL 생성). `metadata.ts`에 `alterTable` 함수 추가(트랜잭션 내 순차 실행, 실패 시 ROLLBACK). `table:alter` IPC 핸들러 등록. preload에 `alterTable` 노출 및 타입 정의 추가.
- 커밋: ⏳ 미커밋

## 테이블 다중선택 + 화살표 expand 분리
- 변경 파일: `src/renderer/src/components/SchemaTree.svelte`
- 내용: 테이블 행에 chevron(화살표) 추가. 화살표 클릭 → 컬럼 목록 펼치기/접기, 테이블명 클릭 → 다중선택. 기존에는 클릭 시 선택만 됐고 화살표도 없어 컬럼 확인 불가했음.
- 커밋: ⏳ 미커밋

## 테이블 선택 방식 개선 (Cmd/Shift 다중선택)
- 변경 파일: `src/renderer/src/components/SchemaTree.svelte`
- 내용: 일반 클릭 → 단일 선택, Cmd/Ctrl+클릭 → 다중 토글, Shift+클릭 → 범위 선택. `lastSelectedTable` 상태 추가해 Shift 기준점 추적.
- 커밋: ⏳ 미커밋

## DDL 복사 다중 선택 지원
- 변경 파일: `src/renderer/src/lib/actions/sidebar-actions.ts`, `src/renderer/src/components/SchemaTree.svelte`
- 내용: `copyDDLMultiple` 함수 추가 — 여러 테이블 DDL을 병렬 조회 후 `\n\n`으로 이어붙여 클립보드에 복사. 우클릭한 테이블이 선택 목록에 포함되고 2개 이상 선택됐을 때 자동으로 다중 복사 경로 사용.
- 커밋: ⏳ 미커밋

## DDL 복사 시 FK 의존성 기반 토폴로지 정렬
- 변경 파일: `src/renderer/src/components/SchemaTree.svelte`
- 내용: `topoSortTables` 함수 추가. 선택된 테이블들의 FK 의존 관계를 DFS로 분석해 참조되는 테이블이 먼저 오도록 정렬. 순환 참조는 visiting set으로 방어 처리. 다중 DDL 복사 시 정렬된 순서로 전달.
- 커밋: ⏳ 미커밋

## 헤더 New Query 버튼 추가
- 변경 파일: `src/renderer/src/components/Header.svelte`, `src/renderer/src/App.svelte`
- 내용: Header에 `onNewTab` prop 추가 및 `SquarePlus` 아이콘 + "New Query" 버튼 추가. App.svelte에서 클릭 시 `newTabDialogOpen = true` 트리거.
- 커밋: ⏳ 미커밋

## 컨텍스트 메뉴 항목 그룹 정리
- 변경 파일: `src/renderer/src/components/SchemaObjectContextMenu.svelte`
- 내용: 메뉴 항목을 조회/편집 → DDL → 관리(수정/새로고침) → 삭제 순으로 separator로 구분.
- 커밋: ⏳ 미커밋

## 데이터 조회 기능 구현
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/lib/stores/tabs.ts`, `src/renderer/src/lib/actions/sidebar-actions.ts`, `src/renderer/src/components/DataViewerTab.svelte`, `src/renderer/src/components/QueryWorkspace.svelte`
- 내용: `selectAll` 함수 추가(PostgreSQL 전용, `SELECT * FROM "schema"."table"` 실행 후 columns/rows 반환). `db:select-all` IPC 핸들러 등록. preload에 `selectAll` 노출 및 타입 추가. `EditorTab`에 `type`/`objectName` 필드 추가. `selectData` 액션 실제 동작 구현(data 타입 탭 오픈). `DataViewerTab` 컴포넌트 신규 생성(로딩/에러/테이블 렌더링, NULL 표시, row count). `QueryWorkspace`에서 탭 타입에 따라 DataViewerTab 또는 textarea 분기 렌더링.
- 커밋: ⏳ 미커밋

## 데이터 조회 기능 구현
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/lib/stores/tabs.ts`, `src/renderer/src/lib/actions/sidebar-actions.ts`, `src/renderer/src/components/QueryWorkspace.svelte`, `src/renderer/src/components/DataViewerTab.svelte`
- 내용: `selectAll` 백엔드 함수 및 `db:select-all` IPC 핸들러 추가. `EditorTab`에 `type('editor'|'data')`, `objectName` 필드 추가. 컨텍스트 메뉴 "데이터 조회" 클릭 시 data 타입 탭으로 열림. `DataViewerTab.svelte` 신규 생성 — sticky 헤더 테이블, NULL 표시, row count. QueryWorkspace에서 탭 타입에 따라 분기 렌더링.
- 커밋: ⏳ 미커밋

## DataViewerTab 툴바 버튼 추가 (데이터 관리 + 페이징)
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 상단 툴바 3구역 구성. 좌: 추가/삭제 버튼, 중: 총 row count, 우: 페이지당 행 수 select + 첫/이전/다음/마지막 페이지 버튼. pageSize/currentPage/totalPages 상태 추가. 버튼 기능은 미구현(UI만).
- 커밋: ⏳ 미커밋

## Cmd/Ctrl+W 탭 닫기 단축키 추가
- 변경 파일: `src/renderer/src/App.svelte`
- 내용: `handleKeydown`에 Cmd/Ctrl+W 처리 추가. 활성 탭을 `tabsStore.closeTab`으로 닫음.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 페이징 기능 구현
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: `pagedRows` derived 추가(rows 슬라이싱). pageSize 변경 시 currentPage=1 리셋($effect). 첫/이전/다음/마지막 페이지 이동 함수 구현 및 버튼 onclick 연결. 기본 pageSize 100으로 변경.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 다중 컬럼 정렬 구현
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: `sortKeys` 상태 추가(누적 정렬 순서 관리). 컬럼 클릭: 없으면 asc 추가, asc면 desc 전환, desc면 제거. `sortedRows` derived로 sortKeys 순서대로 정렬. 헤더 셀에 ArrowUp/ArrowDown 아이콘 + primary 색상으로 정렬 방향 표시, 다중 정렬 시 순서 번호 표시.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 인라인 편집 구현
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 셀 더블클릭 시 input 편집 모드 전환 + 자동 focus/select. Enter/Escape/blur 시 편집 종료. editedData로 변경사항 추적(원본 불변). 변경된 행은 amber 배경, 변경된 셀은 amber 텍스트. 저장/취소 버튼 추가 - 변경사항 없으면 비활성. 저장 버튼은 console.log만(실제 저장 미구현).
- 커밋: ⏳ 미커밋

## 데이터 뷰어 신규 행 입력창 구현
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 추가 버튼 클릭 시 thead 바로 아래 입력 행 표시. 각 컬럼명을 placeholder로 사용. 첫 번째 셀 자동 포커스. Escape로 취소. 취소/저장 버튼에 isAdding/newRow 처리 연동.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 INSERT/UPDATE/DELETE 실제 DB 저장 구현 (PostgreSQL)
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/components/DataViewerTab.svelte`, `src/renderer/src/components/QueryWorkspace.svelte`
- 내용: `selectAll` 반환값에 `primaryKeys: string[]` 추가(pg_constraint 기반 PK 조회). `executeDataChanges` 함수 추가 — BEGIN/COMMIT/ROLLBACK 트랜잭션 안에서 DELETE → UPDATE → INSERT 순 실행, `quoteIdent`로 식별자 처리, `__`로 시작하는 메타 키 제외. `db:data-changes` IPC 핸들러 등록. preload에 `executeDataChanges` 노출 및 타입 추가. `DataViewerTab`에 `dbType` prop 및 `primaryKeys` 상태 추가, `handleSave`를 실제 API 호출로 구현(성공 시 데이터 새로고침, 실패 시 alert). `QueryWorkspace`에서 `dbType` prop 전달.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 삭제 기능 구현 (저장 시 일괄 처리)
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 체크박스 열 추가(헤더 전체선택/해제, 행별 선택). 삭제 버튼 클릭 시 선택 행을 deletedRows에 추가(빨간 배경+취소선으로 표시). 삭제된 행은 더블클릭 편집 불가. 저장 버튼에서 INSERT/UPDATE/DELETE 일괄 처리 예정(현재 console.log). 취소 시 deletedRows/selectedRows 초기화.
- 커밋: ⏳ 미커밋

## 데이터 변경사항 실제 저장 구현 (INSERT/UPDATE/DELETE)
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/components/DataViewerTab.svelte`, `src/renderer/src/components/QueryWorkspace.svelte`
- 내용: `selectAll`에 PK 조회 추가(primaryKeys 반환). `executeDataChanges` 함수 추가 — 트랜잭션 내 DELETE→UPDATE→INSERT 순 실행, `__`로 시작하는 내부 키 제외, quoteIdent로 식별자 처리. `db:data-changes` IPC 핸들러 등록. DataViewerTab에 dbType prop 추가, 저장 성공 시 데이터 새로고침. 현재 PostgreSQL만 지원.
- 커밋: ⏳ 미커밋

## PK 없는 테이블 UPDATE/DELETE 처리 개선
- 변경 파일: `src/main/db/metadata.ts`
- 내용: `buildWhere` 헬퍼 추가. PK가 있으면 PK 컬럼으로 WHERE 구성, 없으면 `__`로 시작하지 않는 전체 컬럼으로 WHERE 구성. 동일 값을 가진 행이 여러 개면 모두 영향받음.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 다중 추가 행 + CSV 익스포트
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: `newRow` 단일 객체 → `newRows` 배열로 변경. 추가 버튼 클릭마다 새 입력 행 추가, 각 행 X 버튼으로 개별 제거, Escape로 해당 행 제거. Export 버튼 추가 — 현재 정렬 기준 전체 데이터를 CSV로 다운로드(특수문자 escape 처리, 파일명: {테이블명}.csv).
- 커밋: ⏳ 미커밋

## Export 드롭다운 추가 (CSV / INSERT SQL)
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: Export 버튼을 DropdownMenu로 교체. CSV(.csv): 헤더 포함, 특수문자 escape. INSERT SQL(.sql): `INSERT INTO "schema"."table" (...) VALUES (...);` 형식, NULL/따옴표 처리. 두 포맷 모두 현재 정렬 기준 전체 데이터 기준.
- 커밋: ⏳ 미커밋

## 선택 row 클립보드 복사 기능 (CSV / INSERT SQL)
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 선택된 row를 CSV 또는 INSERT SQL 형식으로 클립보드에 복사하는 기능 추가. 툴바에 "복사 (N)" 드롭다운 버튼 추가, 선택된 row가 없을 때 비활성화. CSV는 헤더+선택 row, INSERT SQL은 현재 편집값 반영하여 `navigator.clipboard.writeText` 사용.
- 커밋: ⏳ 미커밋

## DataViewerTab 검색 필터 추가
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 툴바에 실시간 검색창 추가. 전체 컬럼 대상 대소문자 무시 필터링. `filteredRows` derived 도입 후 페이징/Export/복사 모두 필터 결과 기준으로 동작. 검색 시 row count를 "N / 전체M개 행"으로 표시. X 버튼으로 검색 초기화.
- 커밋: ⏳ 미커밋

## 트리 선택 버그 수정 (스키마 간 중복 선택, 뷰 선택 미작동)
- 변경 파일: `src/renderer/src/components/SchemaTree.svelte`, `src/renderer/src/components/Sidebar.svelte`
- 내용: SchemaTree 인스턴스가 스키마마다 독립 존재해 selectedTables가 교차 선택되는 버그 수정 — Sidebar에 activeSchemaKey 추적, SchemaTree에 active/onSelect prop 추가 및 $effect로 비활성 시 선택 초기화. 뷰는 단순 toggle만 연결되어 선택 불가하던 문제 수정 — 테이블과 동일하게 chevron/단일클릭(선택)/더블클릭(데이터 열기) 구조로 분리.
- 커밋: ⏳ 미커밋

## 뷰 데이터 조회 시 편집 UI 숨김
- 변경 파일: `src/renderer/src/lib/stores/tabs.ts`, `src/renderer/src/lib/actions/sidebar-actions.ts`, `src/renderer/src/components/QueryWorkspace.svelte`, `src/renderer/src/components/DataViewerTab.svelte`
- 내용: EditorTab에 objectType 필드 추가, selectData 액션에서 objectType 전파, DataViewerTab에 objectType prop 추가 및 readonly 파생 상태 도입. 뷰/matview일 때 저장·취소·추가·삭제 버튼, 체크박스 열, 새 행 입력 영역, 셀 더블클릭 편집 모두 숨김/비활성화.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 컬럼 너비 리사이즈
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 헤더 오른쪽 끝 드래그 핸들로 컬럼 너비 조절. colWidths/resizing state 추가, startResize 함수에서 mousemove/mouseup 이벤트로 너비 계산. 드래그 중 body cursor/userSelect 처리. 최소 너비 40px.
- 커밋: ⏳ 미커밋

## 서버 사이드 정렬/페이징/검색 구현
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`
- 내용: `SelectAllParams` 인터페이스 추가(limit/offset/orderBy/search). `selectAll` 함수를 서버 사이드 처리로 교체 — `information_schema.columns`로 컬럼 목록 조회, search 시 전체 컬럼 CAST TEXT ILIKE 조건 추가, orderBy로 ORDER BY 절 동적 생성, limit>0일 때 LIMIT/OFFSET 적용, COUNT(*) 쿼리로 totalCount 반환. IPC 핸들러에 params 인자 추가. preload selectAll에 params 인자 추가 및 타입 정의 업데이트(totalCount 포함).
- 커밋: ⏳ 미커밋

## 데이터 뷰어 서버 사이드 정렬/페이징/검색
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/components/DataViewerTab.svelte`
- 내용: selectAll에 SelectAllParams(limit/offset/orderBy/search) 추가, COUNT(*) 쿼리로 totalCount 반환, 검색은 전체 컬럼 CAST AS TEXT ILIKE. 프론트엔드에서 클라이언트 정렬/필터/페이징 derived 제거, load() 함수로 통합. 페이지/정렬/pageSize 변경 시 서버 재요청, 검색은 300ms debounce. 페이지 이동 시 미저장 변경사항 자동 초기화.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 NULL/빈 문자열 시각적 구분
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 빈 문자열("")을 NULL과 구분하여 `empty` 배지(회색 배경, 소문자)로 표시. NULL은 기존 이탤릭 스타일 유지.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 JSON 컬럼 포맷터
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: tryParseJson 헬퍼로 셀 값이 JSON인지 파싱 시도(객체/배열만). JSON이면 `{}` 배지 표시 및 클릭 시 prettified 모달 오픈. 모달 내 복사 버튼(1.5초 후 복사됨 피드백), 오버레이 클릭/X 버튼으로 닫기.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 변경 내역 툴팁
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 편집된 셀(changed && !isDeleted)에 title 속성 추가. hover 시 "원래 값: ..." 네이티브 툴팁 표시. 원래 값이 NULL이면 "NULL", 빈 문자열이면 "(빈 문자열)"로 구분 표시.
- 커밋: ⏳ 미커밋

## 데이터 뷰어 새로고침 버튼
- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 툴바 우측에 RefreshCw 아이콘 버튼 추가. 미저장 변경사항 있으면 confirm 후 clearEdits + load() 호출.
- 커밋: ⏳ 미커밋
