## 행 추가 시 기본값 자동 채움

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `selectAll` 함수의 `colResult` 쿼리에 `column_default` 컬럼 추가, `columnDefaults` 맵 생성 후 반환값에 포함

- 변경 파일: `src/preload/index.d.ts`
- 내용: `selectAll` 반환 타입에 `columnDefaults: Record<string, string | null>` 추가

- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: `columnDefaults` 상태 추가, `load()`에서 저장, `handleAddClick`에서 시퀀스(`nextval(...)`) 제외한 기본값을 새 행 초기값으로 채우도록 수정

- 커밋: ⏳ 미커밋

## 체크박스 컬럼 sticky 고정 (헤더 높이 수정 포함)

- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 체크박스 th/td에 `position: sticky; left: 0` 적용. 헤더 높이 불일치 해결을 위해 `div` wrapper + `py-1.5` 추가
- 커밋: ⏳ 미커밋

## 데이터 뷰어 편집/표시 기능 추가

- 변경 파일: `src/main/db/metadata.ts`, `src/preload/index.d.ts`, `src/renderer/src/components/DataViewerTab.svelte`
- 내용:
  - **행 번호**: 각 행 왼쪽에 순번 표시 (sticky 고정, offset 기반 계산)
  - **행 복제**: 선택 행의 PK 제외 데이터를 newRows로 복사하는 복제 버튼 추가
  - **NULL 명시 설정**: 셀 편집 중 Ctrl+Delete로 null 저장, null 상태 시 placeholder 표시
  - **키보드 네비게이션**: Tab/Shift+Tab으로 셀 이동, Enter로 다음 행 같은 컬럼으로 이동
  - **boolean 렌더링**: bool 타입 컬럼은 체크박스로 표시/편집
  - **timestamp 포맷**: timestamp/date 컬럼은 ko-KR locale 포맷으로 표시
  - `columnTypes` 상태 추가, `selectAll` API에 `udt_name` 포함
- 커밋: ⏳ 미커밋

## 컬럼 헤더 우클릭 컨텍스트 메뉴 + 컬럼 고정

- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: 컬럼 헤더 우클릭 시 컨텍스트 메뉴(정렬 오름/내림/해제, 고정 체크박스) 표시. 고정된 컬럼은 `displayColumns` derived로 왼쪽 우선 배치, CSS `position: sticky`로 좌우 스크롤 시 고정. 마지막 고정 컬럼 오른쪽 구분선 표시. 다중 고정 지원.
- 커밋: ⏳ 미커밋
## SQL 편집기 헤더 툴바 추가
- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`, `src/renderer/src/components/QueryWorkspace.svelte`
- 내용: QueryEditor를 독립 컴포넌트로 분리하고 DataViewerTab과 동일한 스타일의 헤더 툴바 추가. 실행 버튼(Play 아이콘 + 단축키 표시)과 스키마 정보 표시 영역 포함. QueryWorkspace에서 인라인 textarea 대신 QueryEditor 컴포넌트 사용으로 변경.
- 커밋: ⏳ 미커밋

## SQL 에디터 결과창 컴포넌트 구현

- 변경 파일: `src/renderer/src/components/QueryResultViewer.svelte`
- 내용: SQL 쿼리 실행 결과를 표시하는 신규 컴포넌트 생성. DataViewerTab의 읽기 전용 뷰 기반으로 클라이언트 사이드 정렬/필터/페이지네이션, 컬럼 고정, 컬럼 리사이즈, 다중 정렬(우선순위 숫자 표시), JSON 모달, 행 선택/CSV 복사/Export, idle/running/error/success 상태별 렌더링 포함
- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: paneforge 기반 수직 분할 레이아웃으로 변경. 에디터(60%)와 QueryResultViewer(40%) 수직 분할. resultStatus/resultColumns/resultRows/resultError/resultExecutionTime/resultAffectedRows 상태 추가
- 커밋: ⏳ 미커밋

## 쿼리 실행 + 포맷팅 기능 구현
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/components/QueryEditor.svelte`
- 내용: `executeQuery` IPC 핸들러 추가 (PostgreSQL/MySQL/SQLite 지원), 실행 버튼 + `Cmd/Ctrl+Enter` 단축키 연결, 선택 영역만 실행, `sql-formatter` 패키지로 포맷 버튼 추가 (dbType별 dialect)
- 커밋: ⏳ 미커밋

## 쿼리 에디터 테이블/컬럼 자동완성 수정

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `getCompletionSchema` 함수 추가. PostgreSQL/MySQL/SQLite 전체 지원. PostgreSQL은 `schemaName` 미전달 시 `public` 기본값 사용. MySQL은 connection의 `databaseName` 사용. SQLite는 `sqlite_master` + `PRAGMA table_info` 사용.

- 변경 파일: `src/main/index.ts`
- 내용: `db:completion-schema` IPC 핸들러 추가

- 변경 파일: `src/preload/index.ts`, `src/preload/index.d.ts`
- 내용: `getCompletionSchema` API 노출 및 타입 선언 추가

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: `getSchemaObjects` 조건부 호출 → `getCompletionSchema` 무조건 호출로 교체. `schemaName` 없이 탭을 열어도 자동완성 작동

- 커밋: ⏳ 미커밋

## CodeMirror 6 에디터 도입 + 에러 위치 표시 + 스키마 자동완성
- 변경 파일: `src/main/db/metadata.ts`, `src/preload/index.d.ts`, `src/renderer/src/components/QueryEditor.svelte`
- 내용: CodeMirror 6으로 textarea 교체 (라인 번호, 구문 하이라이팅, 커서/선택 스타일 CSS 변수 기반). `executeQuery` 반환 타입을 discriminated union(`ok: true/false`)으로 변경해 에러 `position` 전달. linter 확장으로 에러 발생 위치에 빨간 물결 표시. `getSchemaObjects` 결과로 테이블·컬럼 자동완성 구성. 라이트/다크 테마 전환 시 하이라이트 스타일 동적 교체(Compartment). 패키지: `@codemirror/view`, `@codemirror/state`, `@codemirror/commands`, `@codemirror/lang-sql`, `@codemirror/language`, `@codemirror/lint`, `@codemirror/theme-one-dark`
- 커밋: ⏳ 미커밋

## 스키마 자동완성 버그 수정

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `getCompletionSchema` PostgreSQL 케이스 — `schemaName` 미전달 시 `public` 한정 조회에서 사용자 정의 스키마 전체 조회(`pg_catalog`, `information_schema`, `pg_toast` 제외)로 변경. `table_schema` 컬럼 추가 후 `"schema.table"` 형식 키도 함께 등록해 스키마명 자동완성 지원 (`schema` → `table` → `column` 계층). `schemaName` 있을 때도 동일하게 qualified key 추가.

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: 스키마 로드 실패 시 `catch {}` → `catch(err) { console.error(...) }` 로 변경하여 실패 원인이 콘솔에 출력되도록 수정.

- 커밋: ⏳ 미커밋

## 쿼리 실행 히스토리 백엔드 구현

- 변경 파일: `src/main/db/app-db.ts`
- 내용: `initAppDb` 내 `db.exec`에 `query_history` 테이블 DDL 추가. `QueryHistoryRecord` 인터페이스, `addQueryHistory`(INSERT + 100건 초과 시 오래된 행 DELETE), `getQueryHistory`(최근 100건 DESC 조회) 함수 추가.

- 변경 파일: `src/main/index.ts`
- 내용: `app-db`에서 `addQueryHistory`, `getQueryHistory` import 추가. `history:add`, `history:list` IPC 핸들러 등록.

- 커밋: ⏳ 미커밋

## 쿼리 실행 히스토리 프론트엔드 구현

- 변경 파일: `src/preload/index.d.ts`
- 내용: `QueryHistoryRecord` 인터페이스 추가. `DbApi`에 `addQueryHistory`, `getQueryHistory` 메서드 타입 선언 추가.

- 변경 파일: `src/preload/index.ts`
- 내용: `addQueryHistory`(`history:add`), `getQueryHistory`(`history:list`) IPC 브릿지 메서드 추가.

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: `History` 아이콘 import 추가. `historyOpen`, `historyItems` 상태 추가. `loadHistory`, `loadHistoryItem`, `formatRelativeTime` 함수 추가. `runQuery` 완료 후 `addQueryHistory` 호출. 툴바에 히스토리 버튼 및 드롭다운 패널 추가 (성공/실패 표시, 상대 시간, SQL 미리보기, 클릭 시 에디터 로드).

- 커밋: ⏳ 미커밋

## 쿼리 실행 히스토리
- 변경 파일: `src/main/db/app-db.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/components/QueryEditor.svelte`
- 내용: 쿼리 실행 히스토리 기능 구현. app-db SQLite에 query_history 테이블 추가, 실행 성공/실패 시 자동 저장(최대 100건), 툴바 히스토리 버튼 클릭 시 드롭다운 패널에서 조회·재사용
- 커밋: ⏳ 미커밋

## 다중 쿼리 분리 실행

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: 기존 단일 결과 상태 변수(resultStatus 등 6개)를 `QueryResult` 인터페이스 기반 `multiResults` 배열로 교체. `;` 기준 쿼리 분리 함수 `splitQueries` 추가 (문자열/주석 내 세미콜론 무시). `runQuery`를 순차 실행 루프로 전면 교체. 결과창에 쿼리별 탭(상태 인디케이터 포함) 표시. "오류 시 중단" 체크박스 토글 추가.
- 커밋: ⏳ 미커밋

## 다중 쿼리 분리 실행
- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: `;` 기준으로 쿼리 분리 실행 구현. 문자열/주석 내 세미콜론 무시하는 파서 추가, 결과창 쿼리별 탭 표시, "오류 시 중단" 토글 옵션 추가, 기존 단일 결과 상태 변수를 multiResults 배열로 통합
- 커밋: ⏳ 미커밋

## 다중 쿼리 배치 실행 함수 추가 (연결 재사용)

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `BatchQueryResult` 타입 추가, `executeQueryBatch` 함수 추가. DB 연결 1개로 전체 쿼리를 처리하며 PostgreSQL/MySQL/SQLite 각각 구현. `stopOnError` 플래그로 실패 시 잔여 쿼리를 `skipped: true` 로 채우고 중단.

- 변경 파일: `src/main/index.ts`
- 내용: `executeQueryBatch` import 추가, `db:execute-query-batch` IPC 핸들러 등록.

- 커밋: ⏳ 미커밋

## 다중 쿼리 배치 실행 API 프론트엔드 연결

- 변경 파일: `src/preload/index.d.ts`
- 내용: `DbApi` 인터페이스에 `executeQueryBatch` 메서드 타입 선언 추가

- 변경 파일: `src/preload/index.ts`
- 내용: `executeQueryBatch` IPC 브릿지 메서드 추가 (`db:execute-query-batch` 채널)

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: `runQuery`를 단일/다중 분기 방식으로 교체. 쿼리 1개는 기존 `executeQuery` 유지, 2개 이상은 신규 `executeQueryBatch` 사용 (DB 연결 1개 재사용). 배치 결과의 `skipped` 항목은 히스토리 저장 제외. 첫 오류 탭으로 자동 포커스.

- 커밋: ⏳ 미커밋

## 다중 쿼리 배치 실행 (연결 재사용)
- 변경 파일: `src/main/db/metadata.ts`, `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, `src/renderer/src/components/QueryEditor.svelte`
- 내용: 다중 쿼리 실행 시 DB 연결 1개로 처리. executeQueryBatch 추가 (PostgreSQL/MySQL/SQLite 각각 연결 1회 open → 순차 실행 → close). 단일 쿼리는 기존 executeQuery 유지. skipped 항목은 히스토리 저장 제외
- 커밋: ⏳ 미커밋

## 쿼리 에디터 분할 기능 구현

- 변경 파일: `src/renderer/src/lib/stores/tabs.ts`
- 내용: `EditorTab` 인터페이스에 `split`, `splitQuery` 필드 추가. `toggleSplit`, `updateSplitQuery` 메서드 추가.

- 변경 파일: `src/renderer/src/components/QueryWorkspace.svelte`
- 내용: 탭 바 우측에 분할 버튼(`Columns2` 아이콘) 추가. 분할 상태일 때 paneforge 기반 좌우 `PaneGroup`으로 `QueryEditor` 2개 렌더링. 각 에디터는 독립 쿼리/결과 상태 유지, `connectionId`/`schemaName` 공유.

- 커밋: ⏳ 미커밋

## 에디터 분할
- 변경 파일: `src/renderer/src/lib/stores/tabs.ts`, `src/renderer/src/components/QueryWorkspace.svelte`
- 내용: 탭 바 우측 Columns2 버튼으로 에디터 좌우 분할. EditorTab에 split/splitQuery 상태 추가, 분할 시 PaneGroup horizontal로 QueryEditor 2개 렌더링. 각 에디터 독립 쿼리/결과 상태 유지
- 커밋: ⏳ 미커밋

## 키보드 단축키 팔레트 (Command Palette)

- 변경 파일: `src/renderer/src/lib/stores/commands.ts`
- 내용: 명령어 레지스트리 신규 생성. 정적 명령어(새 탭, 쿼리 실행, 포맷, 히스토리, 테마 3종)와 동적 연결 전환 명령어(connections 스토어에서 생성) 포함. 에디터 명령은 window CustomEvent 디스패치. fuzzyMatch 유틸 함수 포함.

- 변경 파일: `src/renderer/src/components/CommandPalette.svelte`
- 내용: 신규 컴포넌트 생성. Cmd+K로 열리는 모달 팔레트. 검색 input + fuzzy 필터 목록, ↑↓ 키보드 탐색, Enter 실행, Escape 닫기. 마우스 호버 시 선택 인덱스 동기화.

- 변경 파일: `src/renderer/src/App.svelte`
- 내용: Cmd+K 전역 단축키 추가, CommandPalette 마운트, palette:new-tab 이벤트 수신해 newTabDialogOpen 토글.

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: palette:run-query / palette:format-query / palette:open-history 윈도우 이벤트 수신 $effect 추가. 팔레트에서 에디터 명령 실행 가능.

- 커밋: ⏳ 미커밋

## 쿼리 취소 및 트랜잭션 제어 기능 추가

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `buildMysqlConfig` 헬퍼 함수 추출 (기존 인라인 MySQL 설정 중복 제거). `runningQueryMap`으로 실행 중인 쿼리의 PostgreSQL pid / MySQL connId 추적. `executeQuery`에서 pid/connId 저장 및 finally에서 삭제. `BatchQueryResult` 타입에 `skipped` 필드 유지, `BatchQueryResponse` 신규 타입 추가 (`results` + `transactionResult`). `executeQueryBatch` 시그니처에 `useTransaction` 파라미터 추가, 각 DB별 트랜잭션 처리 (BEGIN/COMMIT/ROLLBACK) 및 pid/connId 추적 추가. `cancelQuery` 함수 추가 (PostgreSQL: `pg_cancel_backend`, MySQL: `KILL QUERY`).

- 변경 파일: `src/main/index.ts`
- 내용: `cancelQuery` import 추가. `db:execute-query-batch` 핸들러에 `useTransaction` 파라미터 추가. `query:cancel` IPC 핸들러 등록.

- 변경 파일: `src/preload/index.ts`
- 내용: `executeQueryBatch`에 `useTransaction` 파라미터 추가. `cancelQuery` 브릿지 메서드 추가.

- 변경 파일: `src/preload/index.d.ts`
- 내용: `DbApi`에 `cancelQuery` 타입 선언 추가. `executeQueryBatch` 반환 타입을 `BatchQueryResponse` 구조(`results` + `transactionResult?`)로 교체.

- 커밋: ⏳ 미커밋

## 결과 행 수 제한 경고 (LIMIT 누락 경고)

- 변경 파일: `src/renderer/src/components/QueryResultViewer.svelte`
- 내용: `sql` prop 추가. `warningDismissed` 상태 + `sql` 변경 시 자동 리셋 `$effect` 추가. `showWarning` derived: SELECT이고 LIMIT 없을 때 true. 툴바 아래 amber 경고 배너 삽입 (TriangleAlert 아이콘 + 안내 문구 + X 닫기 버튼). 다중 쿼리 탭 전환 시 각 탭 독립 동작.

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: 단일/다중 쿼리 모두 `QueryResultViewer`에 `sql={result.sql}` prop 전달.

- 커밋: ⏳ 미커밋

## 쿼리 취소 (cancel) + 트랜잭션 제어

**쿼리 취소**
- 변경 파일: `src/main/db/metadata.ts`
- 내용: `buildMysqlConfig` 헬퍼 추출. `runningQueryMap` 모듈 레벨 Map 추가. `executeQuery` PostgreSQL/MySQL case에 실행 전 pid/connId 획득 후 map 저장, finally에서 삭제. `cancelQuery` 함수 추가 — PostgreSQL은 `pg_cancel_backend`, MySQL은 `KILL QUERY` 별도 연결로 실행. SQLite 취소 불가.

- 변경 파일: `src/main/index.ts`
- 내용: `cancelQuery` import, `query:cancel` IPC 핸들러 추가, `db:execute-query-batch` 핸들러에 `useTransaction` 파라미터 추가.

- 변경 파일: `src/preload/index.ts`, `src/preload/index.d.ts`
- 내용: `cancelQuery` API 노출 및 타입 선언 추가.

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: `Square` 아이콘 import 추가. 실행 중일 때 실행 버튼 대신 취소 버튼 표시 (SQLite는 비활성화). 취소 클릭 시 `window.api.cancelQuery(connectionId)` 호출.

**트랜잭션 제어**
- 변경 파일: `src/main/db/metadata.ts`
- 내용: `BatchQueryResponse` 타입 추가 (`results` + `transactionResult?`). `executeQueryBatch`에 `useTransaction: boolean = false` 파라미터 추가. PostgreSQL(`BEGIN/COMMIT/ROLLBACK`), MySQL(`beginTransaction/commit/rollback`), SQLite(`BEGIN/COMMIT/ROLLBACK`) 각각 트랜잭션 처리. batch에도 pid/connId 추적 추가.

- 변경 파일: `src/preload/index.ts`, `src/preload/index.d.ts`
- 내용: `executeQueryBatch` 시그니처에 `useTransaction` 추가, 반환 타입을 `BatchQueryResponse` 구조로 교체.

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: `useTransaction`, `transactionResult` 상태 추가. `queryCount` derived (splitQueries 기반). 툴바에 "트랜잭션" 체크박스 추가 (단일 쿼리 시 비활성화). `runQuery`에서 배치 응답 구조 변경 반영. 다중 결과 탭 헤더에 커밋/롤백 뱃지 표시.

- 커밋: ⏳ 미커밋

## EXPLAIN 기능 추가

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `ExplainNode` 인터페이스, `ExplainResult` 타입, normalize 헬퍼(PostgreSQL/MySQL/SQLite 각각), `explainQuery` 함수를 파일 끝에 추가. PostgreSQL은 `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`, MySQL은 `EXPLAIN FORMAT=JSON`, SQLite는 `EXPLAIN QUERY PLAN` 사용.

- 변경 파일: `src/main/index.ts`
- 내용: `explainQuery` import 추가. `query:explain` IPC 핸들러 등록.

- 변경 파일: `src/preload/index.ts`
- 내용: `explainQuery` IPC 브릿지 메서드 추가 (`query:explain` 채널).

- 변경 파일: `src/preload/index.d.ts`
- 내용: `ExplainNode` 인터페이스 추가. `DbApi`에 `explainQuery` 메서드 타입 선언 추가.

- 커밋: ⏳ 미커밋

## EXPLAIN 시각화

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `ExplainNode` 인터페이스, `ExplainResult` 타입 추가. PostgreSQL(`EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`), MySQL(`EXPLAIN FORMAT=JSON`), SQLite(`EXPLAIN QUERY PLAN`) 정규화 헬퍼 4개 추가. `explainQuery` 함수 추가. SELECT 쿼리만 진입 가능 (호출부에서 제어).

- 변경 파일: `src/main/index.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`
- 내용: `query:explain` IPC 핸들러 + `explainQuery` API 브릿지 + `ExplainNode` 타입 선언 추가.

- 변경 파일: `src/renderer/src/components/ExplainTreeNode.svelte` (신규)
- 내용: 재귀 트리 노드 컴포넌트. 전체 maxTime 대비 노드 비율로 red/amber 히트맵 표시. 접기/펼치기. actualTime×loops 계산. Index Name/Filter/Hash Condition 등 extra 필드 표시.

- 변경 파일: `src/renderer/src/components/ExplainViewer.svelte` (신규)
- 내용: 상단에 총 실행 시간 + 색상 범례 표시. ExplainTreeNode 루트 렌더링.

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: ExplainViewer import 추가. `isSelectQuery`, `explainEnabled`, `activeResultView`, `explainResult`, `explainRunning`, `explainError` 상태 추가. `runExplain` 함수 추가. 실행 시 `activeResultView = 'result'` 자동 전환. 결과 영역 상단에 "결과 / 실행 계획" 탭 추가. EXPLAIN 버튼 추가 (SELECT 단일 쿼리일 때만 활성화).

- 커밋: ⏳ 미커밋

## executeQuery/executeQueryBatch columnTypes 추가

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `QueryResult`, `BatchQueryResult` 타입 ok:true 브랜치에 `columnTypes: Record<string, string>` 필드 추가. `buildSqliteTree` 앞에 `pgOidToCategory`, `mysqlTypeToCategory`, `sqliteTypeToCategory` 헬퍼 3개 추가. `executeQuery` PostgreSQL/MySQL/SQLite 각 case에서 `columnTypes` 생성 후 반환값에 포함. `executeQueryBatch` 동일하게 각 DB case 성공 push에 `columnTypes` 포함.

- 변경 파일: `src/preload/index.d.ts`
- 내용: `executeQuery`, `executeQueryBatch` ok:true 반환 타입에 `columnTypes: Record<string, string>` 추가.

- 커밋: ⏳ 미커밋

## 타입별 색상 구분

- 변경 파일: `src/main/db/metadata.ts`
- 내용: `pgOidToCategory`, `mysqlTypeToCategory`, `sqliteTypeToCategory` 헬퍼 추가. `QueryResult` / `BatchQueryResult` ok:true 브랜치에 `columnTypes: Record<string, string>` 추가. `executeQuery` 및 `executeQueryBatch` 3개 DB case 모두 columnTypes 빌드 후 반환. SQLite는 `stmt.columns()`로 빈 결과에서도 컬럼 정보 추출.

- 변경 파일: `src/preload/index.d.ts`
- 내용: `executeQuery`, `executeQueryBatch` 반환 타입 ok:true 브랜치에 `columnTypes` 추가.

- 변경 파일: `src/renderer/src/components/QueryEditor.svelte`
- 내용: `QueryResult` 인터페이스에 `columnTypes` 추가. 초기화/단일/배치 결과 모두 columnTypes 포함. `QueryResultViewer`에 `columnTypes` prop 전달 (2곳).

- 변경 파일: `src/renderer/src/components/QueryResultViewer.svelte`
- 내용: `columnTypes` prop 추가. `getCellClass(col, value)` 함수 추가 — numeric(파란색), datetime(보라색), boolean(true=초록/false=빨강), 기타 기본 색상. 셀 `<span>`에 클래스 적용. NULL/empty는 기존 표시 유지.

- 커밋: ⏳ 미커밋

## DataViewerTab 타입별 색상 구분 적용

- 변경 파일: `src/renderer/src/components/DataViewerTab.svelte`
- 내용: `getCellClass` 함수 추가 (udt_name raw 값 기반 numeric 감지). timestamp 셀에 `cell-datetime`, numeric 셀에 `cell-numeric` 클래스 적용. 삭제/수정 상태 셀은 기존 색상 우선.
- 커밋: ⏳ 미커밋

## 네이티브 메뉴바 개편

- 변경 파일: `src/main/index.ts`
- 내용: 플랫폼 분기(`isMac`) 적용. 앱 메뉴(서비스/숨기기 등 macOS 전용)는 macOS만 포함. 편집 메뉴 제거. 파일 메뉴 추가(새 쿼리 탭 Cmd/Ctrl+T, 탭 닫기 Cmd/Ctrl+W). 보기 메뉴는 개발자 도구+전체 화면만. 윈도우 메뉴는 최소화+맨 앞으로(macOS)/최소화(Windows). `autoHideMenuBar: false`로 통일. 메뉴 클릭 시 `webContents.send`로 IPC 전달.

- 변경 파일: `src/preload/index.ts`, `src/preload/index.d.ts`
- 내용: `onMenuNewTab`, `onMenuCloseTab` IPC 리스너 브릿지 추가.

- 변경 파일: `src/renderer/src/App.svelte`
- 내용: `$effect`에서 `window.api.onMenuNewTab`/`onMenuCloseTab` 등록. 메뉴 클릭 시 새 탭 다이얼로그 열기 및 활성 탭 닫기 동작 연결.

- 커밋: ⏳ 미커밋

## ER 다이어그램 기능 구현

- 변경 파일: `src/renderer/src/lib/stores/tabs.ts`
- 내용: `EditorTab.type`에 `'erd'` 추가. `openTab` params 타입에도 `'erd'` 포함.

- 변경 파일: `src/renderer/src/components/ErDiagramTab.svelte` (신규)
- 내용: `@dagrejs/dagre` 기반 레이아웃 + SVG 직접 렌더링. `getSchemaObjects`로 테이블·FK 정보 로드 후 dagre 그래프 노드/엣지 구성 → layout() → 위치 계산. 각 테이블 카드(헤더+컬럼 목록, PK 표시), FK 관계선(화살표), 마우스 휠 줌/드래그 패닝 지원. 툴바에 확대/축소/초기화 버튼.

- 변경 파일: `src/renderer/src/components/QueryWorkspace.svelte`
- 내용: `ErDiagramTab` import 추가. `activeTab.type === 'erd'` 케이스 추가.

- 변경 파일: `src/renderer/src/components/ConnectionContextMenu.svelte`
- 내용: `onOpenErDiagram` optional prop 추가. `level === 'schema' && dbType === 'postgresql'`일 때 "ER 다이어그램 보기" 메뉴 항목 표시.

- 변경 파일: `src/renderer/src/components/Sidebar.svelte`
- 내용: `tabsStore` import 추가. 스키마 컨텍스트 메뉴에 `onOpenErDiagram` 핸들러 연결 — `tabsStore.openTab({ type: 'erd', title: 'ERD: {schemaName}' })`.

- 커밋: ⏳ 미커밋

## README 기능 현황 업데이트

- 변경 파일: `README.md`
- 내용: 기존 설계 문서 중심 README를 구현된 기능 기준으로 전면 재작성. 연결 관리·스키마 탐색기·SQL 에디터·쿼리 실행·결과/데이터 뷰어·EXPLAIN·ER 다이어그램·히스토리·UX 항목 정리. DB별 지원 현황 테이블 추가.
- 커밋: ⏳ 미커밋
